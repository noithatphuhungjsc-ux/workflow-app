/* ================================================================
   PROJECT MODALS — Re-export for backward compatibility
   App.jsx imports: m.NewProjectModal, m.ProjectDetailSheet
   ================================================================ */
export { default as NewProjectModal } from "./project/NewProjectModal";
export { default as ProjectDetailSheet } from "./project/ProjectDetailSheet";
