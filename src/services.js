/* ================================================================
   SERVICES — Barrel re-export (split into src/services/ modules)
   This file exists for backward compatibility so that
   import { ... } from "./services" continues to work.
   ================================================================ */
export * from "./services/index";
